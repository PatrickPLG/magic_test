<%# magic_test 1.0: the recorder is injected by Rack middleware under MAGIC_TEST.
    This partial stays as a no-op so existing `render 'magic_test/support'` lines
    in host layouts (Studiz renders it from ~12 layouts, twice in mail layouts)
    keep working and render nothing. %>
