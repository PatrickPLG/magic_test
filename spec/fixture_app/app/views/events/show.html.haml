%h1= @event.name
.event-description!= @event.description
%p= @event.starts_at&.strftime("%d/%m-%Y %H:%M")
= link_to t("events.show.signup"), event_path(@event, signup: 1), class: "btn btn-primary"
= link_to t("events.show.continue_to_payment"), event_path(@event, pay: 1), class: "btn btn-success"
